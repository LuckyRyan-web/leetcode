/**
 * @author $user
 * @date $datetime
 * @since ${pkg.version}
 */

interface ExampleType {
    [key: string]: any
    target: any
}

const example: ExampleType[] = []

test('', () => {
    example.forEach((v) => {
        
    })
})
