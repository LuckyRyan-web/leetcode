/**
 * @author $user
 * @date $datetime
 * @since ${pkg.version}
 */

const example_${ref.ModuleName}:Types.ExampleType[] = []

test('', () => {
    example_${ref.ModuleName}.forEach((v) => {
        
    })
})
